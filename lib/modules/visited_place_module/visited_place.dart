import 'package:flutter/material.dart';

class VisitedPlaceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Visited Place Screen',
      ),
    );
  }
}
